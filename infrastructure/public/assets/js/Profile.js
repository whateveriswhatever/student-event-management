document.addEventListener("DOMContentLoaded", () => {
  const folderPath = "/final-project/infrastructure";

  var calendarEl = document.getElementById("schedule-calendar");
  fetch(`${folderPath}/profile`);
});
